import React from "react";

/**
 * Affiche la section "mon rôle précis" sur le projet.
 * Coquille vide pour l'instant : implémentation complète prévue à l'étape 3
 * de la carte Trello, une fois le champ `role` ajouté dans portfolioData.js
 * et le texte rédigé à partir du dossier RNCP.
 */

const StarRole = ({ role }) => {
    return (
        <section className="w-full">
            {/* TODO étape 3 : rédaction et affichage du rôle */}
        </section>
    );
};

export default StarRole;