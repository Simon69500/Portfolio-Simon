import React from "react";

/**
 * Affiche les métriques produit (chiffres clés d'usage du site en production).
 * Coquille vide pour l'instant : implémentation complète prévue à l'étape 2
 * de la carte Trello, une fois le champ `metrics` ajouté dans portfolioData.js.
 */

const StarMetrics = ({ metrics }) => {
    return (
        <section className="w-full">
            {/* TODO étape 2 : affichage des chiffres clés (240 estimations, 96 prospects uniques, etc.) */}
        </section>
    );
};

export default StarMetrics;